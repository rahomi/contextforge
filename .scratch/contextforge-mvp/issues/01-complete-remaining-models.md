"# 01 — Complete Remaining Backend Models

**What to build:** Create 4 missing Sequelize model files (AIConversation, AIMessage, AIEmbedding, EngineeringActivity) following the exact patterns of the 7 existing models and the database schema defined in architecture.md. Register all models in models/index.js with proper associations.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] Create `AIConversation.js` model with fields, associations, indexes
- [ ] Create `AIMessage.js` model with fields, associations, indexes  
- [ ] Create `AIEmbedding.js` model with fields, associations, indexes
- [ ] Create `EngineeringActivity.js` model with fields, associations, indexes
- [ ] Register all models and verify associations in `models/index.js`
- [ ] Verify User.js associations reference all new models
- [ ] Verify Repository.js associations reference all new models"
